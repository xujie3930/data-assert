spring:
  #  cloud:
  #    nacos:
  #      discovery:
  #        server-addr: 192.168.110.118:8848
  #        namespace: dd8ede24-19c7-4547-8956-a566f800a823
  datasource:
    dynamic:
      primary: master
      strict: false
      datasource:
        master:
          url: "jdbc:mysql://192.168.211.2:3306/daas?useSSL=false&useUnicode=true&characterEncoding=utf8"
          username: 'daas'
          password: 'daas@1122'
        remote:
          url: "jdbc:mysql://192.168.141.116:3306/faren?useSSL=false&useUnicode=true&characterEncoding=utf8"
          username: 'farenrw'
          password: 'NJMPSC7u'

  redis:
    host: 192.168.211.2 # Redis服务器地址
    database: 0 # Redis数据库索引（默认为0）
    port: 6379 # Redis服务器连接端口
    timeout: 3000ms # 连接超时时间（毫秒）
    password: VQ1eWwR3

# swagger 登录认证
knife4j:
  basic:
    enable: false
    ## Basic认证用户名
    username: admin
    ## Basic认证密码l
    password: admin123456
  production: false #默认是false ,屏蔽所有Swagger的相关资源
  enable: true #是否开启swagger
logging:
  #  file: ../logs/api-gateway/api-gateway.log
  #  level:
  #    root: info
  config: classpath:logback/logback-dev.xml
  level:
    com.hashtech: info
